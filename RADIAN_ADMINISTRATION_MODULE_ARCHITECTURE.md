# Radian — Administration module architecture

_খসড়া, ৩০ জুলাই ২০২৬। মালিকের পর্যালোচনার জন্য। **লক না হওয়া পর্যন্ত কোনো কোড নয়।**_

---

## ০. এই নথিটা কীসের ভিত্তিতে

কিকঅফ ফাইলের সংখ্যাগুলো কোডে যাচাই করা হয়েছে। **একটা ভুল পাওয়া গেছে**, আর
নতুন কয়েকটা সংখ্যা গোনা হয়েছে:

| যা গোনা হলো | সংখ্যা | কোথায় |
|---|---|---|
| API-তে `@Roles` ব্যবহার | **৭৩** | ১৭টা controller |
| হাতে লেখা `actor.role` চেক | **১০** | ৫টা ফাইল |
| সাইডবারে পর্দা (`href`) | **১৫৯** | `AdminSidebar.tsx` |
| তার মধ্যে `roles:` লেখা আছে | **১১** | বাকি ১৪৮টার কোনো নিয়ম নেই |
| `/settings`-এ সত্যিকারের পর্দা | **৪** | audit · me · people · seo |

### ⚠️ কিকঅফ §২ যেখানে ভুল

> *"Auth-এ শূন্য — `POST /auth/users` আর `DELETE /auth/users/:id`-এ কোনো
> `@Roles` নেই।"*

`@Roles` নেই — সত্যি। **কিন্তু পাহারা আছে।** `auth.controller.ts`-এ হাতে লেখা
একটা helper, চারটে user route-ই ডাকে:

```ts
private ownerOnly(req: AuthedRequest) {
  if (req.actor?.role !== 'OWNER')
    throw new ForbiddenException('Only an owner can manage who has access');
}
```

**STAFF নতুন ইউজার বানাতে পারে না।** গোনাটা শুধু `@Roles` খুঁজেছিল।

**এর থেকে শেখার জিনিসটা সংখ্যাটার চেয়ে বড়:** একই সিদ্ধান্ত আজ **তিন ভাবে**
লেখা যায় — `@Roles` decorator, হাতে লেখা `if`, আর সাইডবারের `roles:` array।
তাই "কে কী পারে" প্রশ্নের কোনো নির্ভরযোগ্য উত্তর নেই। **এই module সেটাই ঠিক
করবে।**

### §৩-এও একটা সংশোধন

কিকঅফ বলেছে ভূমিকার নিয়ম কোথাও এক জায়গায় লেখা নেই। **লেখা আছে** —
`auth.guard.ts`-এর ২১–৩১ লাইনের মন্তব্যে, আর সেটা পরিষ্কার ও সঠিক। সমস্যা
লেখা নেই তা নয় — **লেখাটা প্রয়োগ হয়নি।**

---

## ১. Overview — এই module কেন আছে

আজ ব্যবসার নিয়ন্ত্রণ **কোডের ভেতরে**। মালিক নিজে কিছু বদলাতে পারেন না — কাকে
কী দেখাব, সেটা বদলাতে হলে ডেভেলপার লাগে, কোড বদলাতে হয়, deploy করতে হয়।

Administration সেই নিয়ন্ত্রণটা **কোড থেকে পর্দায়** নিয়ে আসে।

**একটা বাক্যে:** এক জায়গা থেকে ঠিক হবে সিস্টেমে কী কী আছে, কে কোনটা পাবে, আর
কোম্পানি নিজে কে।

---

## ২. দায়িত্ব — এবং সীমানা

### এই module যা করবে

| | কাজ |
|---|---|
| ক | সব module → sub-module → পর্দার **একটাই তালিকা** (registry) |
| খ | কে কোনটা পাবে — **পদ** ও **ব্যক্তি** দুই স্তরে |
| গ | লগইন অ্যাকাউন্ট — তৈরি, নিষ্ক্রিয়, পাসওয়ার্ড ও PIN রিসেট |
| ঘ | কোম্পানির নিজের পরিচয় — লাইসেন্স, BIN, TIN, লোগো |
| ঙ | সব setting-এ পৌঁছানোর এক পর্দা |
| চ | কে কখন কী বদলাল — Audit |
| ছ | ব্যাকআপের অবস্থা, চালু সেশন |
| জ | বাইরের সেবার চাবি |

### ⛔ এই module যা করবে **না** — সীমানা

| যা করবে না | কেন | কার |
|---|---|---|
| কর্মীর তথ্য, বেতন, হাজিরা | HR-D01 — **লগইন অ্যাকাউন্ট আর কর্মী এক জিনিস নয়** | HR |
| VAT-এর হার, POS-এর ছাড়ের সীমা | ওগুলো ব্যবসায়িক নিয়ম, প্রশাসনিক নয় | যার যার module |
| সব setting এক টেবিলে জড়ো করা | **One Data One Owner ভাঙে** | — |
| Workflow builder, SSO, দুই-ধাপের যাচাই | আট জনের দোকানে সাজসজ্জা | — |

**সবচেয়ে জরুরি সীমানা:** রাঁধুনি বেতন পান কিন্তু কখনো লগইন করেন না;
হিসাবরক্ষক লগইন করেন কিন্তু পে-রোলে নেই। `Employee` **ঐচ্ছিক ভাবে** `AppUser`-এর
সাথে যুক্ত — এই সীমাটা ভাঙা যাবে না।

---

## ৩. যে সিদ্ধান্তগুলো নেওয়া হয়েছে

| # | সিদ্ধান্ত | অবস্থা |
|---|---|---|
| ADM-D01 | ভূমিকার নাম **মালিক নিজে বানাবেন** — তিনটে গাঁথা নাম নয় | ✅ লক |
| ADM-D02 | `AppRole` enum → **টেবিলে** সরবে (`Position`) | ✅ লক |
| ADM-D03 | টিক **module + ভেতরের পর্দা** স্তরে | ✅ লক |
| ADM-D04 | পদ = **ছাঁচ**; ব্যক্তির উপরে **ব্যতিক্রম** দেওয়া যাবে | ✅ লক |
| ADM-D05 | registry **কোড থেকে নিজে তৈরি হবে**, হাতে লেখা নয় | ✅ লক |
| ADM-D06 | নতুন পর্দা এলে **বন্ধ, কিন্তু লাল চিহ্ন সহ** | ⚠️ ধরে নেওয়া |
| ADM-D07 | permission matrix (প্রতিটা **কাজের** টিক) — **বানানো হবে না** | ✅ লক |

**ADM-D06 নিয়ে খোলাখুলি:** মালিক বলেছেন *"যা যা update হবে module-এ, সেভাবে
এখানে update হবে"* — অর্থাৎ **তালিকাটা নিজে থেকে হালনাগাদ হবে**, সেটা লক।
কিন্তু নতুন পর্দাটা পুরনো পদগুলো **সাথে সাথে পাবে কি না** — সেটা আলাদা প্রশ্ন,
আর সেখানে আমার সুপারিশ ধরে নেওয়া হয়েছে: **পাবে না, তবে নীরবে নয়।** আপত্তি
থাকলে এখনই বলা দরকার।

---

## ৪. Business entities — এই module যা মালিকানা করে

```prisma
/// সিস্টেমে কী কী আছে — কোড থেকে তৈরি, হাতে লেখা নয় (ADM-D05)
model AccessNode {
  key        String   @id            // "finance.pnl" — স্থির, কখনো বদলাবে না
  parentKey  String?                 // "finance"
  kind       NodeKind                // MODULE | SCREEN
  label      String                  // "Profit & Loss"
  domain     String                  // "Finance"
  href       String?                 // সাইডবার এখান থেকে পড়বে
  routes     String[]                // ["GET /finance/pnl", ...] — পাহারা এখান থেকে
  firstSeenAt DateTime @default(now())  // লাল চিহ্নের ভিত্তি
  retiredAt  DateTime?               // মুছে ফেলা নয় — পুরনো টিক বাঁচিয়ে রাখতে
}

/// পদ — নামটা মালিকের (ADM-D01)
model Position {
  id       String  @id @default(cuid())
  name     String  @unique           // "হিসাবরক্ষক", "ডেলিভারি"
  note     String?
  isOwner  Boolean @default(false)   // সব পায়, কাটা যায় না (ADM-RULE-004)
  isLocked Boolean @default(false)
  access   PositionAccess[]
  users    AppUser[]
}

/// পদের ছাঁচ — শুধু ব্যতিক্রম লেখা থাকে (ADM-D04)
model PositionAccess {
  positionId String
  nodeKey    String
  allowed    Boolean
  @@id([positionId, nodeKey])
}

/// এক ব্যক্তির জন্য আলাদা নিয়ম — ছাঁচের উপরে (ADM-D04)
model UserAccessOverride {
  userId  String
  nodeKey String
  allowed Boolean
  @@id([userId, nodeKey])
}
```

`AppUser` পাবে একটা `positionId`।

**মালিকানা করে না:** `Employee` (HR) · `FinanceSetting`-এর ব্যবসায়িক ঘর
(Finance) · `AuditLog`-এর সারি (যে যে module লেখে)।

---

## ৫. অ্যাক্সেস কীভাবে হিসাব হবে — উপর থেকে নিচে

একজন মানুষ একটা পর্দা পাবে কি না, এই ক্রমে ঠিক হবে। **প্রথম যেটা উত্তর দেয়,
সেটাই চূড়ান্ত:**

```
১. পদটা কি OWNER?                          → হ্যাঁ হলে সব পাবে। শেষ।
২. এই ব্যক্তির জন্য আলাদা নিয়ম লেখা আছে?    → থাকলে সেটাই। শেষ।
৩. পদের ছাঁচে এই পর্দার কথা লেখা আছে?       → থাকলে সেটাই। শেষ।
৪. উপরের module-টা কি পদ পেয়েছে?           → পেলে হ্যাঁ। (উত্তরাধিকার)
৫. কিছুই না?                               → না। (নতুন পর্দা — ADM-D06)
```

**৪ নম্বর ধাপটাই কাজটা সহজ রাখে।** "হিসাবরক্ষক" বানাতে হলে Finance-এ টিক দিলেই
ভেতরের সব পর্দা খুলে যায় — তারপর শুধু "লাভ-ক্ষতি" বন্ধ করে দিলে হয়। **১৫৯টা
নয়, দুটো সিদ্ধান্ত।** কিন্তু ক্ষমতাটা থাকে: যেদিন দরকার, ভেতরে ঢুকে যেকোনো
একটা পর্দা আলাদা করে বন্ধ করা যাবে।

---

## ৬. Business rules

| # | নিয়ম | ব্যতিক্রম |
|---|---|---|
| ADM-RULE-001 | **সাইডবার আর API পাহারা একই registry পড়বে।** দুটো তালিকা আর থাকবে না | **নেই** |
| ADM-RULE-002 | registry কোড থেকে তৈরি হবে; হাতে সম্পাদনা করা যাবে না | নেই |
| ADM-RULE-003 | নতুন node ডিফল্ট বন্ধ, আর Access পর্দায় গোনা সহ সতর্কতা উঠবে | নেই |
| ADM-RULE-004 | OWNER পদ মোছা বা কাটা যাবে না; **শেষ সক্রিয় OWNER নিষ্ক্রিয় করা যাবে না** | নেই |
| ADM-RULE-005 | পদ ও অ্যাক্সেস বদলানো **শুধু OWNER**; প্রতিটা বদল AuditLog-এ | নেই |
| ADM-RULE-006 | **PIN-এর নিয়ম অ্যাক্সেসের উপরে** — অ্যাক্সেস থাকলেও টাকার কাজে PIN লাগবে | **নেই** |
| ADM-RULE-007 | পর্দা যা লুকায়, **API-র উত্তরেও যেন না থাকে** | নেই |
| ADM-RULE-008 | কর্মী মোছা হবে না — নিষ্ক্রিয় (soft-delete); খাতার সারিতে তার নাম আছে | নেই |
| ADM-RULE-009 | নিজের পদ নিজে বদলানো যাবে না — অন্য একজন OWNER করবেন | নেই |

**ADM-RULE-006 আলাদা করে বলা দরকার।** টিক আর PIN দুটো আলাদা জিনিস, আর একটা
আরেকটার বদলি নয়:

> **টিক বলে "সে ওই পর্দাটা দেখতে পাবে।"**
> **PIN বলে "টাকার কাজটা সত্যিই সে-ই করছে।"**

কেউ Finance-এর টিক পেলেও ১৯টা টাকার কাজে PIN লাগবেই। এটা আলগা করা যাবে না।

**ADM-RULE-007-ও তত্ত্ব নয়।** পর্দা লুকানো নিরাপত্তা নয় — API খোলা থাকলে ডেটা
বেরিয়ে যায়। Self-test উত্তরটা JSON করে ভেতরে খুঁজে দেখবে।

---

## ৭. ৩৪৬টা রুট কীভাবে বন্ধ হবে — এবং কেন এক ধাপে নয়

**এখানেই সবচেয়ে বড় ঝুঁকি।** ৩৪৬টা রুটে হঠাৎ পাহারা বসালে **দোকান বন্ধ হয়ে
যেতে পারে** — একটা রুট ভুল করে বন্ধ হলে কেউ অর্ডার নিতে পারবে না, আর সেটা টের
পাওয়া যাবে গ্রাহক দাঁড়িয়ে থাকা অবস্থায়।

তাই তিন ধাপে:

**ধাপ ১ — registry বসানো, পাহারা নয়।**
প্রতিটা রুট নিজের node-এর নাম বলবে। এখনো কিছু বন্ধ হচ্ছে না।

**ধাপ ২ — নীরব পরীক্ষা।** *(এটাই মূল রক্ষাকবচ)*
পাহারা চলবে, কিন্তু **কাউকে আটকাবে না** — শুধু লিখে রাখবে *"এই রুটটা এই মানুষকে
আটকাতাম।"* কয়েক দিন সত্যিকারের কাজের মধ্যে চলবে। **তালিকাটা খালি না হওয়া
পর্যন্ত পরের ধাপে যাওয়া হবে না।**

**ধাপ ৩ — চালু করা।** এবার সত্যিই আটকাবে।

এতে সময় লাগে বেশি, কিন্তু *"বন্ধ করে দেখি কী ভাঙে"* পদ্ধতিতে যেটা ভাঙে সেটা
দোকান।

---

## ৮. পুরনো তিন ভূমিকা থেকে সরে আসা

**প্রথম দিন কারও কিছু বদলাবে না।** তিনটে পদ আগে থেকে বানানো থাকবে, আর তাদের টিক
এমন ভাবে বসানো হবে যাতে আজকের আচরণ **হুবহু** একই থাকে:

| পদ | টিক |
|---|---|
| `OWNER` | সব (`isOwner`, কাটা যায় না) |
| `MANAGER` | Finance, Purchases, Suppliers, Staff সহ — আজ যা পায় |
| `STAFF` | Orders, POS, Inventory, Delivery, Assembly, Returns |

তারপর মালিক ধীরে ধীরে নিজের পদ বানাবেন, নাম বদলাবেন, বা এই তিনটেই রেখে দেবেন।

**enum-টা এক release পর্যন্ত থাকবে** — দুটো একসাথে চলবে, মিলছে কি না দেখা হবে,
তারপর enum বাদ। একবারে ফেলে দিলে ফেরার পথ থাকে না।

---

## ৯. পর্দাগুলো

**`/settings/access`** — ৩ কলাম: বাঁয়ে পদের তালিকা · মাঝে module → পর্দার গাছ
(টিক সহ) · ডানে ওই পদে কারা আছেন। উপরে লাল পটি: *"৩টা নতুন পর্দা এসেছে, কেউ
এখনো পায়নি।"*

**`/settings/people`** — নাম · ইমেইল · পদ · সক্রিয় কি না। খুললে: পাসওয়ার্ড
রিসেট · **PIN রিসেট** (আজ কোনো উপায় নেই) · এই ব্যক্তির ব্যতিক্রমের টিক।

**`/settings/company`** · **`/settings/index`** (hub) · **`/settings/audit`**
(খোঁজা-ছাঁকা সহ) · **`/settings/backup`** · **`/settings/sessions`**

---

## ১০. অন্য module-এর সাথে সম্পর্ক

| Module | সম্পর্ক |
|---|---|
| **সব module** | নিজের node registry-তে ঘোষণা করবে; পাহারা এখান থেকে পাবে |
| **HR** | `Employee` ↔ `AppUser` ঐচ্ছিক যোগ। **HR-এর সীমা অক্ষত থাকবে** |
| **Finance** | পরিচয়ের ৬টা ঘর Finance থেকে Company-তে সরবে ⚠️ **Mushak ৬.৩ আবার পরীক্ষা করতে হবে** |
| **Audit** | সব module লেখে, Administration পড়ার পর্দা দেয় |
| **Intelligence** | ১৬টা `@Roles` আছে — registry-তে সরবে, আচরণ বদলাবে না |
| **Ecommerce** | SSLCommerz-এর চাবি এখান থেকে আসবে |

---

## ১১. যেসব ফাঁদ এই module-এ পড়ার সম্ভাবনা সবচেয়ে বেশি

- **নতুন singleton-এ `upsert` নয়** — `common/singleton.ts` ব্যবহার করতে হবে।
  ২৯ জুলাই আটটা module-এ একই দৌড়-শর্ত ধরা পড়েছিল।
- **`deletedAt` না থাকলে** `soft-delete.extension.ts`-এর `NO_SOFT_DELETE`-এ নাম
  যোগ করতে হবে, নইলে প্রতিটা call ৫০০ দেবে।
- **Nest route ক্রম** — স্থির path সবসময় `:id`-এর উপরে। ছয়বার ধরা পড়েছে।
- **চাবির প্রতিটা ঘরে `autoComplete="new-password"`** — `"off"` Chrome মানে না।
- **refusal টেস্টে প্রত্যাশিত বার্তা বাধ্যতামূলক** — নইলে ভুল কারণে সবুজ হবে
  (Intelligence-এ দুবার হয়েছিল)।
- **যে status কেউ পড়ে না সেটা নিয়ম নয়** — `allowed` ফিল্ডটা সত্যিই পাহারায়
  ব্যবহার হচ্ছে কি না, self-test মিলিয়ে দেখবে।

---

## ১২. Self-test — `administration.selftest.ts`

`intelligence.selftest.ts`-এর আকৃতিতে। যা অবশ্যই পরীক্ষা করবে:

1. **সাইডবারের প্রতিটা `href`-এর একটা node আছে** — ১৫৯টাই
2. **প্রতিটা API রুটের একটা node আছে** — ৪১৯টাই। *এই দুটোই §৪-এর ভুল
   চিরতরে বন্ধ করে*
3. **টিক না থাকলে API সত্যিই ৪০৩ দেয়** — আর বার্তাটাও মিলিয়ে দেখে
4. **লুকানো জিনিস উত্তরের JSON-এ নেই** (ADM-RULE-007)
5. **টিক থাকলেও PIN ছাড়া টাকার কাজ আটকায়** (ADM-RULE-006)
6. **শেষ OWNER নিষ্ক্রিয় করা যায় না** (ADM-RULE-004)
7. **পুরনো তিন পদের আচরণ আজকের সাথে হুবহু মেলে** (§৮)

---

## ১৩. যা এখনো জানা দরকার

| # | প্রশ্ন |
|---|---|
| ১ | **ADM-D06** — নতুন পর্দা ডিফল্ট বন্ধ, এটা ঠিক আছে? |
| ২ | **Company Settings-এ ঠিক কোন ঘরগুলো** লাগবে, আর কোন ফোন/ইমেইল কাগজে ছাপা হবে? |
| ৩ | **Finance থেকে পরিচয়ের ঘর সরানো** — Mushak ৬.৩ আবার পরীক্ষা করতে হবে, রাজি? |
| ৪ | **Audit পর্দায় কী** — "কে কখন কী বদলাল", নাকি "এই অর্ডারটার ইতিহাস"? |
| ৫ | **শুরুতে কোন পদগুলো** বানাব? (নাম বলে দিলে সেগুলো দিয়েই শুরু) |


---

## ADM-D10 — Who may hold an account: both doors, linked (owner, 18 Aug 2026)

**Decision.** Access is always granted by **email invite** (ADM-RULE-006 path:
invite link, the person sets their own password). Two doors lead to that one
path:

1. **From an Employee** — the employee's page gets a "Give access" action.
   An account created this way is **linked** to that Employee row
   (`AppUser.employeeId`). The link carries a rule: when the employment ends
   (Employee goes inactive/terminated), the linked account is deactivated and
   its sessions are killed **automatically**. One switch, not two.
2. **Directly by email** — for people who are not on the payroll (an outside
   accountant, a partner). Same invite, no link, managed by hand.

**Why not employee-only:** the first real outside accountant would have to be
faked into HR. **Why not email-only:** the same human ends up in two books
that never agree, and closing access on exit relies on somebody remembering.

**What this changes in code:** one nullable `AppUser.employeeId` column (+ the
auto-deactivate hook in HR's status transition). Positions/templates apply to
both kinds of account identically — the access model does not care which door
a person came through.
