# Radian — Administration module kickoff

_২৯ জুলাই ২০২৬। Intelligence শেষ হওয়ার পর মালিকের সিদ্ধান্ত: পরের module
**Administration** — যেখান থেকে গোটা ব্যবসার নিয়ন্ত্রণ হবে।_

এই ফাইলটা পরের chat-এর জন্য। যা যা জানা দরকার সব এখানে — যাতে নতুন chat শূন্য
থেকে শুরু না করে, আর আগের ভুলগুলো আবার না হয়।

---

## ০. প্রথমেই যা করতে হবে

পড়ো, এই ক্রমে:

1. `D:\radian\RADIAN_ADMINISTRATION_KICKOFF.md` — এইটা
2. `D:\radian\RADIAN_FINAL_REVISION_TODO.md` — **ইচ্ছে করে যা অসমাপ্ত রাখা হয়েছে**
3. `D:\radian\RADIAN_MODULE_PRIORITY.md` — ৬০ module-এর মানচিত্র
4. `D:\radian\RADIAN_INTELLIGENCE_MODULE_ARCHITECTURE.md` — **§১৪ঙ (পূর্ণ পর্যালোচনা)**
   বিশেষ করে **R1** — গত module-এ নেভিগেশন কীভাবে একটা লক করা সিদ্ধান্ত নীরবে
   উল্টে দিয়েছিল। Administration-এ ঠিক এই ভুলটাই সবচেয়ে সহজে হবে।

Skills লোড করো: `radian-business-context` · `radian-development-context` ·
`module-design-template` · `business-rules-writing` · `architecture-review` ·
`decision-log-writing`।

---

## ১. কাজের নিয়ম (অপরিবর্তিত)

- **বাংলায় কথা, কোড ও UI ইংরেজিতে।**
- **এক এক করে প্রশ্ন**, প্রতিটার সাথে অপশন ও তোমার সুপারিশ।
- **আমি লক করার আগে কোনো কোড নয়।**
- আমি হিসাব-নিকাশ কম বুঝি — **সহজ করে বুঝিয়ে তারপর জিজ্ঞেস করবে**।
- **আমি টার্মিনাল চালাব না** — সব `.bat` ফাইল, নয়তো তুমি নিজে চালিয়ে দেবে।
  (Intelligence-এ Claude নিজেই সব `.bat` চালিয়েছে computer-use দিয়ে — সেটাই
  সবচেয়ে ভালো কাজ করেছে।)
- **`schema.prisma` শুধু তুমি ছোঁবে।**
- **আমি বললেই মেনে নেবে না** — ভুল মনে হলে সরাসরি বলবে।
- **module শেষ হবে self-test দিয়ে**, `intelligence.selftest.ts`-এর আকৃতিতে।
- একসাথে অনেক কিছু বানিয়ে ফেলবে না — **প্রতিটা টুকরোর পর যাচাই**।

---

## ২. এই মুহূর্তে অ্যাক্সেস কীভাবে কাজ করে — সংখ্যায়

এইটাই এই module-এর কেন্দ্রীয় তথ্য। **কোড গুনে বের করা, অনুমান নয়:**

```
API-তে মোট রুট                   ৪১৯
তার মধ্যে ভূমিকার পাহারা আছে       ৭৩
পাহারা নেই                       ৩৪৬
টাকার কাজে PIN লাগে              ১৯
```

**মানে যেকোনো লগইন করা মানুষ ৩৪৬টা রুট ডাকতে পারে।**

module ধরে ধরে:

| Module | রুট | পাহারা | মন্তব্য |
|---|---|---|---|
| Marketing | ৭২ | ৫ | |
| Finance | ৭১ | ১৫ | পুরো controller `OWNER, MANAGER` |
| HR | ৩৩ | ৪ | কর্মীর বেতন, NID |
| Delivery | ২৫ | ১২ | |
| **Inventory** | ২১ | **০** | স্টক, অপচয়, মূল্য |
| Suppliers | ১৮ | ১ | |
| Returns | ১৮ | ২ | টাকা ফেরত |
| **POS** | ১৮ | **০** | নগদ, শিফট |
| Offers | ১৭ | ৯ | |
| Items | ১৭ | ২ | |
| Intelligence | ১৭ | ১৬ | সবচেয়ে ভালো ঢাকা |
| Purchases | ১৫ | ১ | |
| Orders | ১৫ | ১ | |
| Customers | ১২ | ১ | গ্রাহকের ফোন, ঠিকানা |
| Assembly | ১২ | ১ | |
| SEO | ১১ | ১ | |
| **Auth** | ১১ | **০** | ইউজার তৈরি/মোছা |
| Products | ১০ | ১ | |
| Audit | ৬ | ১ | |

⚠️ **Auth-এ শূন্য** — মানে `POST /auth/users` (নতুন ইউজার) আর
`DELETE /auth/users/:id`-এ কোনো `@Roles` নেই। প্যানেলের মেনুতে "People & access"
শুধু OWNER দেখে, **কিন্তু সেটা শুধু মেনু।** নতুন chat নিজে যাচাই করুক — এটা
সত্যি হলে এটাই module-এর প্রথম কাজ।

---

## ৩. ভূমিকা মাত্র তিনটে, আর সেগুলো কোডে গাঁথা

```prisma
enum AppRole {
  OWNER    // দুই অংশীদার — সব পারে, ইউজার ব্যবস্থাপনা সহ
  MANAGER  // বিশ্বাসী কর্মী — টাকার কাজ চলে, ইউজার নয়
  STAFF    // দৈনন্দিন; টাকার কাজে OWNER/MANAGER-এর PIN লাগে
}
```

**কোনো permission টেবিল নেই।** প্রতিটা সিদ্ধান্ত controller-এর ভেতরে একটা
`@Roles(...)` লাইন। ফল দুটো:

১. **"কে কী পারে" প্রশ্নের উত্তর দিতে ১৯টা controller পড়তে হয়।** কোথাও এক
   জায়গায় লেখা নেই।
২. **বদলাতে হলে কোড বদলে deploy করতে হয়।** মালিক নিজে কিছু বদলাতে পারেন না।

---

## ৪. দুটো তালিকা যারা মিলতে বাধ্য, অথচ মেলে না

**ক. সাইডবার** — `AdminSidebar.tsx`-এ হাতে লেখা একটা array, তার নিজস্ব
`roles: [...]`।
**খ. API-র পাহারা** — controller-এ ছড়ানো `@Roles(...)`।

দুটো আলাদা জায়গা, একই প্রশ্নের উত্তর দেয়, আর **কেউ মেলায় না**।

⚠️ **এটা তত্ত্ব নয় — Intelligence বানানোর দিনেই ঘটেছে।** সাইডবারে
`roles: ["OWNER","MANAGER"]` বসানোয় STAFF-এর কাছ থেকে **পুরো Intelligence
লুকিয়ে গিয়েছিল**, অথচ DEC-INT-005 STAFF-কে Executive Dashboard ইচ্ছে করে
দিয়েছিল, আর API দরজা খোলাই রেখেছিল। কোনো error হয়নি। কর্মীরা শুধু তাদের জন্য
লেখা পর্দাটা কোনোদিন পেত না।

**একটা তালিকা থাকলে এটা ঘটত না।** সেটাই এই module-এর সবচেয়ে বড় সুযোগ।

---

## ৫. Settings দশ জায়গায় ছড়ানো

singleton টেবিল, প্রতিটা আলাদা module-এর:

`FinanceSetting` · `MarketingSetting` · `SeoSetting` · `TrackingSetting` ·
`MessagingSetting` · `IntelligenceSetting` · `ReturnSetting` ·
`InventorySetting` · `OfferSetting` · `PosSetting`

আর সাইডবারে **"Settings" নামে একটা এন্ট্রি আছে যার কোনো `href` নেই** — ক্লিক
করলে কিছুই হয় না। মৃত বোতাম।

⚠️ **সাবধান, এখানে একটা লোভনীয় ভুল আছে:** সব setting এক টেবিলে জড়ো করা।
**করা উচিত নয়** — VAT-এর হার Finance-এর ব্যবসায়িক নিয়ম, POS-এর ছাড়ের সীমা
POS-এর। এক টেবিলে আনলে One Data One Owner ভাঙে। **যা দরকার সেটা এক টেবিল নয়,
এক পর্দা** — যেখান থেকে সবগুলোয় পৌঁছানো যায়।

---

## ৬. কোম্পানির নিজের পরিচয় ভুল জায়গায় আছে

```
FinanceSetting.businessBin · businessName · businessAddress ·
businessVatCircle · signatoryName · signatoryDesignation
```

এগুলো Mushak ৬.৩-এর জন্য Finance-এ বসানো হয়েছিল, আর তখন সেটা যুক্তিসঙ্গত ছিল।
**কিন্তু কোম্পানির নাম-ঠিকানা Finance-এর সম্পত্তি নয়** — ওটা কোম্পানির।

আর **যেগুলো একেবারেই নেই:** ট্রেড লাইসেন্স নম্বর ও মেয়াদ · TIN · লোগো ·
যোগাযোগের ফোন/ইমেইল · ব্যবসার ঠিকানা (VAT ঠিকানার থেকে আলাদা হতে পারে)।

`RADIAN_MODULE_PRIORITY.md`-এ Company Settings "প্রায় শেষ" Tier ৩-এ আছে —
**Mushak ৬.৩ শুধু BIN ঘরটার জন্য আটকে আছে।**

---

## ৭. যা ইতিমধ্যে আছে — Administration এগুলোর উপর দাঁড়াবে

| যা আছে | কোথায় | অবস্থা |
|---|---|---|
| লগইন, সেশন, পাসওয়ার্ড (scrypt) | `AppUser` · `AppSession` | কাজ করছে |
| টাকার কাজে ৪-সংখ্যার PIN | `AppUser.pinHash` · `@NeedsPin` | ১৯টা রুটে |
| ইউজার তৈরি/সম্পাদনা/নিষ্ক্রিয় | `/auth/users` · `/settings/people` | কাজ করছে, **পাহারাহীন** |
| প্রতিটা বদলের খাতা | `AuditLog` — প্রথম দিন থেকে জমছে | পর্দা আছে `/settings/audit` |
| নিজের পাসওয়ার্ড/PIN বদল | `/settings/me` | কাজ করছে |
| ভূমিকা তিনটে | `AppRole` enum | কোডে গাঁথা |

**লগইন অ্যাকাউন্ট আর কর্মী এক জিনিস নয়** (HR-D01) — রাঁধুনি বেতন পান কিন্তু
কখনো লগইন করেন না; হিসাবরক্ষক লগইন করেন কিন্তু পে-রোলে নেই। `Employee` ঐচ্ছিক
ভাবে `AppUser`-এর সাথে যুক্ত। **এই সীমাটা রাখতে হবে।**

---

## ৮. আমার সুপারিশ — এবং একটা খোলাখুলি আপত্তি

মালিক বলেছেন: *"পুরো ব্যবসার কন্ট্রোল, সব module ও sub-module এক জায়গায়, কাকে
কোনটার অ্যাক্সেস দেব।"* সেটা পড়ে প্রথমেই যেটা মনে আসে — **একটা permission
matrix**: প্রতিটা module × প্রতিটা কাজ × প্রতিটা ভূমিকা, টিক দেওয়ার ছক।

**আমি এর বিপক্ষে, আর কারণটা বলা দরকার।**

`RADIAN_MODULE_PRIORITY.md`-এর Tier ৫-এ "Approval Workflow"-কে *না বানানোর*
তালিকায় রাখা হয়েছিল এই যুক্তিতে: *"যেটা দরকার সেটা আগেই আছে — তিনটে ভূমিকা,
আর প্রতিটা টাকার কাজে PIN।"* Permission matrix ঠিক একই ফাঁদ, আরও বড় আকারে।
আট জনের দোকানের জন্য একটা অনুমতি-ইঞ্জিন বানানো হয়, একবার সেট করা হয়, তারপর
কেউ আর ছোঁয় না — কিন্তু প্রতিটা নতুন feature-এ ওটা আপডেট করতে ভুলে যায়, আর
তখন ইঞ্জিনটাই মিথ্যে বলে।

**আসল সমস্যা মডেল মোটা দাগের হওয়া নয় — মডেলটা প্রয়োগই হয়নি।** ৪১৯টার মধ্যে
৭৩। Inventory-তে শূন্য। POS-এ শূন্য। Auth-এ শূন্য।

**তাই আমার সুপারিশ, এই ক্রমে:**

**১. একটা registry — সব module ও sub-module এক জায়গায়** *(এটাই মূল কাজ)*
   একটা তালিকা যা বলে: এই module, এর ভেতরে এই পর্দাগুলো, এটা কোন domain-এর,
   কোন ভূমিকা দেখবে, কোন রুটগুলো এর। **সাইডবার এটা পড়বে, পাহারাও এটা পড়বে।**
   তাহলে §৪-এর দুই তালিকা এক হয়ে যায় — আর যে ভুলটা আজ ঘটেছে সেটা আর সম্ভব থাকে
   না। ভবিষ্যতের module যোগ করা মানে এক জায়গায় এক লাইন।

**২. ৩৪৬টা পাহারাহীন রুট বন্ধ করা** — registry থেকেই, একটা করে হাতে নয়।
   এটাই সবচেয়ে বেশি নিরাপত্তা দেয় সবচেয়ে কম কোডে।

**৩. Company Settings** — লাইসেন্স, BIN, TIN, লোগো। Finance থেকে পরিচয়ের
   ঘরগুলো এখানে সরানো (cross-module কাজ, লগ করতে হবে)। **Mushak ৬.৩ এতেই খুলে
   যাবে।**

**৪. Audit পর্দা শক্ত করা** — ডেটা তিন মাস ধরে জমছে, পর্দা আছে। খোঁজা, ছাঁকা,
   "এই সারিটা কে বদলেছিল" — এটুকু হলেই যথেষ্ট।

**৫. Integrations / key store** — SSLCommerz, bKash, WhatsApp, Meta-র চাবি।
   এখন `MessagingSetting` আর `TrackingSetting`-এ ছড়ানো।
   ⚠️ **Chrome `autoComplete="off"` মানে না** — প্রতিটা চাবির ঘরে
   `autoComplete="new-password"` লাগবে, নইলে Chrome সেভ করা পাসওয়ার্ড বসিয়ে
   দেয় (২৯ জুলাই তিনটে পর্দায় ধরা পড়েছিল)।

**যা এখন বানানো উচিত নয়:** permission matrix · workflow builder · SSO ·
দুই-ধাপের যাচাই। আট জনের দোকানে এগুলো সাজসজ্জা।

---

## ৯. মালিককে যা জিজ্ঞেস করতে হবে (এক এক করে)

1. **তিনটে ভূমিকা কি যথেষ্ট?** একটা সত্যিকারের ফাঁক আছে: **হিসাবরক্ষক** —
   Finance দেখা দরকার, কিন্তু অর্ডার বা স্টক ছোঁয়ার দরকার নেই। আজ তাঁকে
   MANAGER করতে হয়, তাতে সব খুলে যায়। *সুপারিশ: চতুর্থ ভূমিকা `ACCOUNTANT`,
   permission engine নয়।*
2. **§৮-এর ১ ও ২ — registry দিয়ে সব রুট বন্ধ করা** — এটাই আগে ধরব কি?
3. **Company Settings-এ কী কী লাগবে?** লাইসেন্স নম্বর ও মেয়াদ · BIN · TIN ·
   লোগো · কোন ফোন/ইমেইল কাগজে ছাপা হবে।
4. **Finance থেকে পরিচয়ের ঘরগুলো সরানো ঠিক আছে কি?** (cross-module, Mushak
   পরীক্ষা করতে হবে)
5. **Audit পর্দায় কী দরকার** — কে, কখন, কী বদলাল? নাকি "এই অর্ডারটার ইতিহাস"?
6. **কর্মী কি নিজের অ্যাকাউন্ট নিজে বন্ধ করতে পারবে?** কেউ চলে গেলে কী হবে —
   নিষ্ক্রিয়, নাকি মুছে ফেলা? *(soft-delete ছাড়া উপায় নেই — খাতার সারিতে তার
   নাম আছে।)*

---

## ১০. যেসব ফাঁদে আগে পড়েছি — আবার যেন না পড়ি

- **সাইডবার আর API আলাদা কথা বলতে পারে।** আজই ঘটেছে (§৪)। মেনু লুকানো
  নিরাপত্তা নয়; পাহারা API-তে হতে হবে। **আর পর্দা যা লুকায়, payload-এও যেন না
  থাকে** — Intelligence-এর self-test JSON সিরিয়ালাইজ করে খোঁজে।
- **singleton টেবিলে `upsert` যথেষ্ট নয়।** ২৯ জুলাই আটটা module-এ একই দৌড়-শর্ত
  পাওয়া গেছে; সমাধান `common/singleton.ts` — নতুন singleton-এ **সেটাই ব্যবহার
  করতে হবে**, হাতে লেখা নয়।
- **singleton-এ `deletedAt` না থাকলে** `soft-delete.extension.ts`-এর
  `NO_SOFT_DELETE`-এ নাম যোগ করতে হবে, নইলে প্রতিটা call ৫০০ দেবে।
- **Nest route ক্রম** — স্থির path সবসময় `:id`-এর উপরে। ছয়বার ধরা পড়েছে।
  Intelligence-এ `?key=` ব্যবহার করে দৃশ্যত এড়ানো হয়েছে।
- **যে self-test কখনো লাল হয়নি, সেটা আবার পড়ো।** Intelligence-এর প্রথম রানে
  দুটো check **ভুল কারণে সবুজ** ছিল — `refuses()` যেকোনো error মেনে নিচ্ছিল।
  **refusal টেস্টে প্রত্যাশিত বার্তা বাধ্যতামূলক করতে হবে।**
- **API sandbox থেকে compile-check করা যায় না** (Prisma client বাসি)। তাই
  `node -e "ts.transpileModule → new vm.Script(...)"` — V8 duplicate `const`
  ধরে, TypeScript parser ধরে না। **আসল গেট
  `radian_check_and_restart.bat`** — ভাঙা কোডে restart করে না, আর Intelligence-এ
  চারবার সেটাই বাঁচিয়েছে।

---

## ১১. চালু রাখার হাতিয়ার

| ফাইল | কাজ |
|---|---|
| `START_RADIAN.bat` | Docker + API + panel — রোজ সকালে এইটা |
| `radian_check_and_restart.bat` | **কোড যাচাই করে তবেই restart** |
| `radian_api_doctor.bat` | কী ভাঙল দেখায় |
| `_grab_api_log.bat` | নিঃশব্দে `_now.log`-এ লগ লেখে |
| `radian_backup.bat` · `_check` · `_schedule` · `_restore` | ব্যাকআপ |
| `radian_marketing_selftest.bat` | ১৬০টা check |
| `radian_intelligence_selftest.bat` | ৮৮টা check |

নতুন migration-এর জন্য **নিজের `.bat` লিখতে হবে**, `radian_intelligence_migrate.bat`-এর
আকৃতিতে — ভেতরে মন্তব্যে **কী যোগ হচ্ছে এবং কেন**, যাতে ছ'মাস পরেও বোঝা যায়।

---

## ১২. এই মুহূর্তে সিস্টেম কোথায় (২৯ জুলাই ২০২৬, রাত)

**তৈরি ও পরীক্ষিত:** Products · Items · Categories · Tags · Brands ·
Customers · Suppliers · Staff/HR · Orders · POS · Returns · Purchases ·
Inventory · Assembly · Delivery · Marketing & Growth · Finance · Offers ·
SEO · Audit · Auth · **Intelligence (সম্পূর্ণ — ৪টা sub-module, ৮৮ check)**

**তৈরি নয়:** **Administration (এই কাজ)** · Ecommerce (storefront ↔ API) ·
Integrations · CMS

**চলছে:** API Docker-এ `:4000` · admin panel `:3001` · Postgres `:5433` ·
storefront `:3000` (**এখনো ১০০% নকল ডেটা — একটাও API call নেই**)

**Intelligence থেকে যা ঝুলে আছে, Administration-এর নয় কিন্তু হারানো চলবে না:**

- **`Order.promisedBy` কেউ ভরে না** → on-time delivery সৎভাবে বলে যে মাপা
  যাচ্ছে না। **Orders-এর কাজ** (TODO §৭খ-ক)।
- **`/delivery/performance` পর্দাটা এখনো `deliveryDemo.ts` পড়ে** — আসল
  endpoint তৈরি (`GET /delivery/performance`), পর্দাটা ঘোরানো বাকি।
- **"সময়মতো নিজে তৈরি রিপোর্ট"** — DEC-INT-004-এ ইচ্ছে করে বাদ।
- **"AI Insights"** — বানানো হয়নি; খাতা খালি থাকলে ওর মানে হয় না।

**সবচেয়ে বড় ঝুলে থাকা কাজ:** এখনো **Ecommerce**। অনলাইনে একটাও অর্ডার সিস্টেমে
ঢোকে না। মালিক ইচ্ছে করে এটা শেষে রেখেছেন — সিদ্ধান্ত তাঁর, কিন্তু নতুন chat
যেন জানে।
