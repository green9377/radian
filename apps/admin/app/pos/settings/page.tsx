import { PosSettings } from "../../_components/PosViews";

/* /pos/settings — POS settings (discount rules, float, receipt, methods, registers). */
export default function PosSettingsPage() {
  return <PosSettings />;
}
