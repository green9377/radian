import { PosOverview } from "../_components/PosViews";

/* /pos — POS module Overview (KPI snapshot). Sell screen lives at /pos/sell. */
export default function PosPage() {
  return <PosOverview />;
}
