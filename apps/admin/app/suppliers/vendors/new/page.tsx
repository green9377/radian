import SupplierEditor from "../../../_components/SupplierEditor";

/* DEC-SUP-009 — same editor, vendor door: Fulfillment type pre-picked,
   notify/lead-time card first. */
export default function VendorNewPage() {
  return <SupplierEditor vendorMode />;
}
