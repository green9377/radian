import CategoryPageView from "../../_components/CategoryPageView";

/* /storefront/category-page — the category page, section by section: what
   shows, what it says, and where each block gets its contents from. */
export default function CategoryPageSettings() {
  return <CategoryPageView />;
}
