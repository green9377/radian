import CollectionsView from "../../_components/CollectionsView";

/* /storefront/collections — the budget cards and any other named shelf. */
export default function CollectionsPage() {
  return <CollectionsView />;
}
