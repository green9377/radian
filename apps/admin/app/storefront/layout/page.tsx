import HomepageView from "../../_components/HomepageView";

/* /storefront/layout — the homepage: layout, banners, trust strip, budget
   cards and wording, behind five tabs on one screen. */
export default function HomepagePage() {
  return <HomepageView />;
}
