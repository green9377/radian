import StorefrontOverview from "../_components/StorefrontOverview";

/* /storefront — the hub. Pages, blocks, words, shop details. */
export default function StorefrontPage() {
  return <StorefrontOverview />;
}
