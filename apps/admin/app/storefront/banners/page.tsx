import BannersView from "../../_components/BannersView";

/* /storefront/banners — the homepage slider, promo strip and announcement line.

   Filed under Storefront rather than Marketing: these are the shop's own front
   window, not a campaign. Marketing owns what is *sent out*; Storefront owns
   what a visitor lands on. Sections, collections, reviews and the footer join
   this group as they are built. */
export default function BannersPage() {
  return <BannersView />;
}
