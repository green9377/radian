import SectionTextView from "../../_components/SectionTextView";

/* /storefront/sections — the three lines above every section of the site. */
export default function SectionsPage() {
  return <SectionTextView />;
}
