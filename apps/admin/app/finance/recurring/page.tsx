import { RecurringLive } from "../../_components/FinanceExtras";

/* /finance/recurring — the same expense every month (G1). */
export default function FinanceRecurringPage() {
  return <RecurringLive />;
}
