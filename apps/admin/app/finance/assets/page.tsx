import { AssetsLoansLive } from "../../_components/FinanceMore";

/* /finance/assets — things we own, money paid in advance, money borrowed. */
export default function FinanceAssetsPage() {
  return <AssetsLoansLive />;
}
