import { ExpensesLive } from "../../_components/FinanceForms";

/* /finance/expenses — manual spend + approval gate (LIVE). */
export default function FinanceExpensesPage() {
  return <ExpensesLive />;
}
