import { AccountantJournalLive } from "../../_components/FinanceExtras";

/* /finance/journal — accountant mode, the only Dr/Cr screen (G6). */
export default function FinanceJournalPage() {
  return <AccountantJournalLive />;
}
