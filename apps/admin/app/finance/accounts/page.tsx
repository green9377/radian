import { AccountsLive } from "../../_components/FinanceLive";

/* /finance/accounts — money accounts + chart of accounts + counting (LIVE). */
export default function FinanceAccountsPage() {
  return <AccountsLive />;
}
