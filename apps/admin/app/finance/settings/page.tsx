import { FinanceSettingsLive } from "../../_components/FinanceLive";

/* /finance/settings — the rules the ledger follows (LIVE). */
export default function FinanceSettingsPage() {
  return <FinanceSettingsLive />;
}
