import { ChartOfAccountsLive } from "../../_components/FinanceChart";

/* /finance/chart — every heading money can sit under, grouped (G3/G4/G5). */
export default function FinanceChartPage() {
  return <ChartOfAccountsLive />;
}
