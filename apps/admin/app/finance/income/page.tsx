import { IncomeLive } from "../../_components/FinanceForms";

/* /finance/income — other income + moving money between our own accounts (LIVE). */
export default function FinanceIncomePage() {
  return <IncomeLive />;
}
