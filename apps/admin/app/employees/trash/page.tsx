import { EmployeeTrashView } from "../../_components/EmployeeViews";

/* /employees/trash — soft-deleted staff, and the way back (HR-R02). */
export default function EmployeeTrashPage() {
  return <EmployeeTrashView />;
}
