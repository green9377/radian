import { EmployeeRolesView } from "../../_components/EmployeeViews";

/* /employees/roles — the job-role master (HR-D10). */
export default function EmployeeRolesPage() {
  return <EmployeeRolesView />;
}
