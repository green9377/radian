import ProductListView from "../../_components/ProductListView";

/* /products/list — All products (live from :4000 /products). */
export default function ProductsListPage() {
  return <ProductListView />;
}
