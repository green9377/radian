import { CampaignsView } from "../../../_components/MarketingViews";

/* /marketing/campaigns/list — every campaign, and the form to add one. */
export default function CampaignsListPage() {
  return <CampaignsView />;
}
