import { OrderSourcesView } from "../../../_components/MarketingHub";

/* /marketing/campaigns/sources — the five-rung attribution ladder (MKT-D02),
   and the count of orders whose origin nobody knows (MKT-D03). */
export default function OrderSourcesPage() {
  return <OrderSourcesView />;
}
