import { TrackingView } from "../../_components/TrackingView";

/* /marketing/tracking — every pixel and tag id in one place (MKT-D15). */
export default function TrackingPage() {
  return <TrackingView />;
}
