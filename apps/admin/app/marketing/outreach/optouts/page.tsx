import { OptOutsView } from "../../../_components/MarketingViews";

/* /marketing/outreach/optouts — people who asked not to be contacted
   (MKT-RULE-009). No exception applies to anyone on this list. */
export default function OptOutsPage() {
  return <OptOutsView />;
}
