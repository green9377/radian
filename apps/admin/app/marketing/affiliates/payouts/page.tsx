import { PayoutsView } from "../../../_components/MarketingHub";

/* /marketing/affiliates/payouts — every time money left for an outsider. */
export default function PayoutsPage() {
  return <PayoutsView />;
}
