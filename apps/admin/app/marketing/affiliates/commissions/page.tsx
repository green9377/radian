import { CommissionsView } from "../../../_components/MarketingHub";

/* /marketing/affiliates/commissions — one row per order, per affiliate. */
export default function CommissionsPage() {
  return <CommissionsView />;
}
