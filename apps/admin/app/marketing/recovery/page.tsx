import RecoveryView from "../../_components/RecoveryView";

/* /marketing/recovery — failed payments and unfinished checkouts. */
export default function RecoveryPage() {
  return <RecoveryView />;
}
