import { MarketingSettingsView } from "../../_components/MarketingViews";

/* /marketing/settings — commission rate, hold days, reminder lead, message. */
export default function MarketingSettingsPage() {
  return <MarketingSettingsView />;
}
