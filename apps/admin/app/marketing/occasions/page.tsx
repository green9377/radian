import { OccasionsView } from "../../_components/MarketingViews";

/* /marketing/occasions — the reminder list, plus one-click WhatsApp (MKT-D07). */
export default function OccasionsPage() {
  return <OccasionsView />;
}
