import { OffersSettingsLive } from "../../../_components/OffersLive";

/* /offers/settings — LIVE engine settings (threshold, default combinable). */
export default function OffersSettingsPage() {
  return <OffersSettingsLive />;
}
