import { OffersListLive } from "../../../_components/OffersLive";

/* /offers/list — All Offers (LIVE, :4000 /offers — DEC-OFR-001..009). */
export default function OffersListPage() {
  return <OffersListLive />;
}
