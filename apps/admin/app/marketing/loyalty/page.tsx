import { LoyaltyView } from "../../_components/LoyaltyView";

/* /marketing/loyalty — points on ordinary purchases (MKT-D21).
   Earned on delivered, spent inside a 20 % cap, off until the
   radianbd.com balances are seeded. */
export default function MarketingLoyaltyPage() {
  return <LoyaltyView />;
}
