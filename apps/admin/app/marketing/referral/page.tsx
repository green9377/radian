import { ReferralView } from "../../_components/ReferralView";

/* /marketing/referral — a customer brings a friend (MKT-D16).
   Points to the referrer, a discount to the friend, and the points ledger
   that Loyalty will later share. */
export default function ReferralPage() {
  return <ReferralView />;
}
