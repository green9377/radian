import { AdsView } from "../../_components/AdsView";

/* /marketing/ads — what Meta charged and what it bought (MKT-D20).
   Read-only: the ledger is never written from here. */
export default function MarketingAdsPage() {
  return <AdsView />;
}
