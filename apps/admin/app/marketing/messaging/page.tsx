import { MessagingView } from "../../_components/MessagingView";

/* /marketing/messaging — Email & SMS (MKT-D19). Provider-agnostic; nothing
   sends until a channel is switched on and a key is saved. */
export default function MessagingPage() {
  return <MessagingView />;
}
