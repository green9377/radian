import { WhatsappView } from "../../_components/WhatsappView";

/* /marketing/whatsapp — templates and send lists (MKT-D18).
   No API needed; a send still writes an Outreach row. */
export default function WhatsappPage() {
  return <WhatsappView />;
}
