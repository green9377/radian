import { ReturnSettingsView } from "../../_components/ReturnViews";

/* /returns/settings — return reasons + policy settings (DEC-RTN-009/014). */
export default function ReturnSettingsPage() {
  return <ReturnSettingsView />;
}
