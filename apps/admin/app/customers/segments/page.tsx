import CustomerSegments from "../../_components/CustomerSegments";

/* /customers/segments — segment (customer group) master. */
export default function CustomerSegmentsPage() {
  return <CustomerSegments />;
}
