import CustomerConsent from "../../_components/CustomerConsent";

/* /customers/consent — communication permission per customer. */
export default function CustomerConsentPage() {
  return <CustomerConsent />;
}
