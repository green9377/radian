import CustomerListView from "../../_components/CustomerListView";

/*
  /customers/list — the full customer table (search / country / segment / status).
*/
export default function CustomersListPage() {
  return <CustomerListView />;
}
