import CustomerRisk from "../../_components/CustomerRisk";

/* /customers/risk — risk levels, fake-COD watch and the blocklist. */
export default function CustomerRiskPage() {
  return <CustomerRisk />;
}
