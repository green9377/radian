import OrderListView from "../../_components/OrderListView";

/* /orders/list — every online order, searchable and filterable. */
export default function OrdersListPage() {
  return <OrderListView />;
}
