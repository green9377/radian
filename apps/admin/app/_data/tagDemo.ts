/*
  DEPRECATED — superseded by `tagGroupDemo.ts`.

  The Occasions & Tags module moved from a flat two-type list (OCCASION|RECIPIENT)
  to dynamic tag GROUPS + per-tag images. Demo data now lives in `tagGroupDemo.ts`
  and the API client in `api.ts` (loadTagsSafe / listTagGroups / listAllTags).

  Kept only as an empty module so no stale imports break the build.
  ⇄ SAFE TO DELETE once you confirm nothing imports it.
*/
export {};
