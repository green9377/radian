import Integrations from "../../../_components/Integrations";

export const metadata = { title: "Messaging — Radian Admin" };

export default function Page() {
  return <Integrations only="MESSAGING" />;
}
