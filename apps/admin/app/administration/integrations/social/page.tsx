import Integrations from "../../../_components/Integrations";

export const metadata = { title: "Social & ads — Radian Admin" };

export default function Page() {
  return <Integrations only="SOCIAL" />;
}
