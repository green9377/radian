import Integrations from "../../../_components/Integrations";

export const metadata = { title: "Courier & delivery keys — Radian Admin" };

export default function Page() {
  return <Integrations only="COURIER" />;
}
