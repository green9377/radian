import Integrations from "../../../_components/Integrations";

export const metadata = { title: "Tracking & analytics — Radian Admin" };

export default function Page() {
  return <Integrations only="ANALYTICS" />;
}
