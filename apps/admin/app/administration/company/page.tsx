import CompanySettings from "../../_components/CompanySettings";

export const metadata = { title: "Company settings — Radian Admin" };

export default function Page() {
  return <CompanySettings />;
}
