import { SettingsMapScreen } from "../../_components/AdminSystem";

export const metadata = { title: "All settings — Radian Admin" };

export default function Page() {
  return <SettingsMapScreen />;
}
